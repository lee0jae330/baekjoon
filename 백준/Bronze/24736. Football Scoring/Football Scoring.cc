#include<iostream>
#include<cmath>

using namespace std;

int main(void) {
	int a, b, c, d, e;
	cin >> a >> b >> c >> d >> e;
	int sum1 = a * 6 + 3 * b + 2 * c + d + 2 * e;
	cin >> a >> b >> c >> d >> e;
	cout << sum1 <<' ' << a * 6 + 3 * b + 2 * c + d + 2 * e << '\n';
	return 0;
}
